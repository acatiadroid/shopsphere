# ShopSphere Product-Catalog Azure Function Environment Variables
# Copy this file to .env for local development

# Database Configuration
DB_SERVER=luke-shopsphere.database.windows.net
DB_NAME=shopsphere
DB_USER=your_db_username
DB_PASSWORD=your_db_password

# Azure Storage Configuration (Required for image uploads)
# Choose one option:

# Option 1: Connection String (Recommended)
# Get from: Azure Portal → Storage Account → Access Keys → Connection string
SqlConnectionString=DRIVER={MySQL ODBC 8.0 Driver};SERVER=localhost;PORT=3306;DATABASE=shopsphere_db;UID=root;PWD=password
# Option 2: Account Key (Alternative)
# Get from: Azure Portal → Storage Account → Access Keys → Key
# AZURE_STORAGE_ACCOUNT_KEY=your_account_key_here

# Storage Account Details (defaults - usually no need to change)
# STORAGE_ACCOUNT=shopsphere
# CONTAINER_NAME=cdn

# User Auth Service URL (for session verification)
USER_AUTH_URL=https://user-auth-feh2gugugngnbxbp.norwayeast-01.azurewebsites.net/api

# Notes:
# - The connection string includes the account key, so you only need ONE of the two options above
# - For Azure deployment, set these as Application Settings in the Azure Portal
# - Never commit the actual .env file to version control
